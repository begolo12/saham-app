fastapi>=0.115
uvicorn>=0.30
mangum>=0.19
yfinance>=0.2
pandas>=2.2
numpy>=1.26
pydantic>=2
psycopg[binary]>=3.2
python-multipart>=0.0.9
# Optional cache: fakeredis provides in-process Redis-compatible cache for the
# BackgroundWorker on serverless hosts (Vercel) where a real Redis isn't set.
# Falls back gracefully to no-cache if even fakeredis fails to import.
fakeredis>=2.20

# Password hashing — required by services/db.py. Without this, the app silently
# falls back to a static-salt SHA-256, which is brute-forceable on a GPU.
bcrypt>=4.0
